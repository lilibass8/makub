# حل مشكلة "العملية غير مسموحة" في التسجيل

## 🔴 المشكلة
عند محاولة التسجيل، يظهر خطأ: **"العملية غير مسموحة" / "Operation not allowed"**

## ✅ الحل (3 خطوات)

---

## الخطوة 1: تثبيت Firebase Package

**المشكلة:** Firebase package غير مثبت!

**الحل:**
```bash
npm install firebase
```

انتظر حتى ينتهي التثبيت، ثم أعد تشغيل:
```bash
npm run dev
```

---

## الخطوة 2: تفعيل Email/Password في Firebase Console

هذه **أهم خطوة**! بدونها لن يعمل التسجيل.

### التعليمات:

1. **افتح Firebase Console:**
   - اذهب إلى: https://console.firebase.google.com
   - اختر مشروع `bellabox`

2. **تفعيل Authentication:**
   - من القائمة الجانبية → **Build** → **Authentication**
   - اضغط على **Get Started** (إذا لم تفعّل Authentication بعد)

3. **تفعيل Email/Password:**
   - اضغط على تبويب **Sign-in method**
   - ابحث عن **Email/Password** في القائمة
   - اضغط على **Email/Password**
   - فعّل الخيار الأول: **Email/Password** (Enable)
   - اضغط **Save**

### صورة توضيحية للخطوات:
```
Firebase Console
├── Authentication
    ├── Sign-in method
        ├── Email/Password ← اضغط هنا
            ├── [✓] Enable ← فعّل هذا
            └── Save ← احفظ
```

---

## الخطوة 3: إنشاء Collections في Firestore

### طريقة 1: تلقائياً (موصى بها)

Collections ستُنشأ **تلقائياً** عند أول استخدام! لا تحتاج لعمل شيء.

عندما:
- تسجل مستخدم جديد → ينشئ `users` collection
- تضيف حجز → ينشئ `bookings` collection
- وهكذا...

### طريقة 2: يدوياً في Firebase Console

إذا أردت إنشاءها مسبقاً:

1. **افتح Firestore:**
   - Firebase Console → **Firestore Database**
   - إذا لم تُنشئ database بعد، اضغط **Create database**
   - اختر **Start in production mode**
   - اختر Location: `nam5` (أو أقرب منطقة لعُمان)
   - اضغط **Enable**

2. **إنشاء Collections:**
   - اضغط **Start collection**
   - أدخل Collection ID: `users`
   - اضغط **Next**
   - أدخل Document ID مؤقت: `temp`
   - أضف حقل: `test` = `true`
   - اضغط **Save**
   - يمكنك حذف هذا Document بعدها

3. **كرر للـ Collections الأخرى:**
   - `artists`
   - `bookings`
   - `spaces`
   - `spaceBookings`

---

## 🧪 اختبار التسجيل

بعد إتمام الخطوات السابقة:

1. **افتح التطبيق:**
   ```bash
   npm run dev
   ```

2. **اذهب للتسجيل:**
   - افتح المتصفح: `http://localhost:5173/register`

3. **املأ البيانات:**
   - الاسم الكامل
   - البريد الإلكتروني
   - رقم هاتف عماني (مثال: `+968 91234567`)
   - اختر المنطقة
   - كلمة المرور (6 أحرف على الأقل)
   - اختر نوع الحساب

4. **اضغط إنشاء حساب**

5. **✅ يجب أن يعمل الآن!**

---

## ✅ التحقق من نجاح التسجيل

### في Firebase Console:

1. **Authentication:**
   - اذهب إلى **Authentication** → **Users**
   - ✅ يجب أن ترى المستخدم الجديد

2. **Firestore:**
   - اذهب إلى **Firestore Database**
   - ✅ يجب أن ترى collection `users`
   - ✅ افتحها، يجب أن يكون هناك document بـ uid المستخدم

---

## 🔍 استكشاف الأخطاء

### إذا استمرت المشكلة:

#### 1. تحقق من Console في المتصفح:
```
F12 → Console
```
ابحث عن رسائل الخطأ الحمراء

#### 2. الأخطاء الشائعة:

**"auth/operation-not-allowed"**
- ✅ الحل: فعّل Email/Password في Firebase Console (الخطوة 2)

**"Firebase: Error (auth/invalid-api-key)"**
- ✅ الحل: تحقق من `firebase.ts` أن apiKey صحيح

**"Cannot find module 'firebase/app'"**
- ✅ الحل: نفذ `npm install firebase`

**"auth/weak-password"**
- ✅ الحل: كلمة المرور يجب أن تكون 6 أحرف على الأقل

**"auth/invalid-email"**
- ✅ الحل: استخدم إيميل صحيح (example@email.com)

---

## 📋 Checklist

قبل التسجيل، تأكد من:

- [ ] تم تثبيت Firebase: `npm install firebase`
- [ ] تم تفعيل Email/Password في Firebase Console
- [ ] Firestore Database تم إنشاءه (أو سيُنشأ تلقائياً)
- [ ] التطبيق يعمل: `npm run dev`
- [ ] لا توجد أخطاء في Browser Console

---

## 🎯 الخلاصة

**السبب الرئيسي:** Email/Password authentication غير مفعّل في Firebase Console.

**الحل السريع:**
1. ثبّت Firebase: `npm install firebase`
2. فعّل Email/Password في Firebase Console
3. أعد تشغيل التطبيق
4. جرّب التسجيل

**Collections:** ستُنشأ تلقائياً عند أول استخدام!

---

## 📞 إذا احتجت مساعدة

أرسل screenshot من:
1. Browser Console (F12 → Console)
2. رسالة الخطأ
3. Firebase Console → Authentication → Sign-in method

وسأساعدك في حل المشكلة! 🚀
